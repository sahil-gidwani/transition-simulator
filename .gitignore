# Local-only working docs
*.pdf
PROMPTS.md

# Environment
.env
.env.local

# Node
node_modules/
dist/
coverage/
*.tsbuildinfo

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Data (large raw source files; processed artifacts ARE committed)
server/data/raw/

# Backtest outputs (reproducible via `uv run python -m pipeline.eval`)
server/data/eval/

# Extra CA certificates for docker builds (local-only; see docker/certs/README.md)
docker/certs/*
!docker/certs/README.md

# Local design scratch space (screenshots, logo concept sketches)
.scratch/

# OS / editor
.DS_Store
Thumbs.db

# Submission meta, never committed
docs/video-script.md
